# ShardedLobbyCore - Main Configuration
# Reload with /shardedlobbycore reload

prefix: "&8[&bShardedLobbyCore&8] &r"

# All modules are configured in the modules/ folder.
# Set enabled: false in any module file to disable it.

settings:
  debug: false
