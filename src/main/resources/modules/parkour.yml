# Parkour Module
# Click the configured block to start parkour (not an inventory item)

enabled: true

block:
  material: LIME_GLAZED_TERRACOTTA

blocks:
  - LIME_GLAZED_TERRACOTTA

command: "ajparkour start"
