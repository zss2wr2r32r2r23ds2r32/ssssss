# Pickup spawners module
# Permission: sharded.spawners.use (pay), sharded.spawners.pickup (mine spawners)
# Pay with: /spawners pay [amount]
config-version: 3
# message-mode: inherit  # chat | actionbar | both — overrides global message-mode
price: 100
use-tokens: true
require-silk-touch: true
