config-version: 3
message-mode: chat

# World used when portal RTP module is disabled
fallback-world: world

# Open spawn selector on first join (optional — closing without picking defaults to vanilla)
prompt-on-join: false
prompt-delay-ticks: 40

# Fallback RTP radius if portal RTP is disabled (normally uses portalrtp/config.yml)
rtp-fallback:
  center-x: 0
  center-z: 0
  min-radius: 100
  max-radius: 500
  max-attempts: 25

# main-spawn and vanilla-world are set via /setspawn
